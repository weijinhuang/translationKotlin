spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/hwjtranslation?rewriteBatchedStatements=true&useServerPrepStmts=true
    username: javasql
    password: 123456
  # 新增HTTP请求/响应编码配置
  http:
    encoding:
      charset: UTF-8
      force: true

server:
  port: 80
  address: 0.0.0.0
  # 新增Servlet容器编码配置
  servlet:
    encoding:
      charset: UTF-8
      force: true
  # 新增Tomcat URI编码配置（处理URL中的中文参数）
  tomcat:
    uri-encoding: UTF-8